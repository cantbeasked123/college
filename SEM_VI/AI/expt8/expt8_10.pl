suffers(charlie,fever).
suffers(charlie,running_nose).
suffers(charlie,headache).

suffers(micky,cough).
suffers(micky,headache).
suffers(micky,running_nose).
suffers(micky,sneezing).

suffers(branda,fever).
suffers(branda,rash).

flu(headache).
flu(body_ache).
flu(fever).
flu(running_nose).
flu(cough).
flu(conjunctivitis).

measles(fever).
measles(cough).
measles(conjunctivitis).
measles(running_nose).
measles(rash).

whooping_cough(cough).
whooping_cough(sneezing).
whooping_cough(running_nose).